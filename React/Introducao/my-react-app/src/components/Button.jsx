import React from 'react'

const Button = (props) => {

    const { name, onClick } = props          // Desestruturação ES6. Mesma coisa que 'const name = props.name'

    return {

        <button></button >

    }
}

export default Button
