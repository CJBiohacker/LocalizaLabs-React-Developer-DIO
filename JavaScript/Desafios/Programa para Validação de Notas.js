var input = parseInt(gets());

const notaValida = input <= 10 && value >= 0;                   // Define as condições de intervalo da entrada da nota. "value <= 10 OU value >= 0"

let conjuntoNotas = [];

while (true) {

    if (notaValida) {
        /*
            Se o valor for entre 0 a 10, inserir 'i'
            na matriz 'arr', caso contrário exibir
            a mensagem de erro do desafio.
    
            Se a matriz tiver 2 valores, então: calcular,
            zerar a matriz e mudar o 'status' para false.
        */
    } else {
        /*
            Após algum cálculo deverá ser exibida
            a mensagem de novo cálculo.
            Daí caso a próxima entrada venha '1', mudar
            o 'status' para true e se for '2' então
            o laço while deve ser quebrado.
        */
    }
}